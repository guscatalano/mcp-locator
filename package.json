{
  "name": "mcp-locator-monorepo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @mcp-locator/client -w @mcp-locator/gateway --if-present",
    "test": "npm test --workspaces --if-present",
    "validate": "node packages/schema/scripts/validate.mjs examples conformance"
  },
  "engines": {
    "node": ">=20.10"
  }
}
